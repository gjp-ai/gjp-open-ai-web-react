# Use relative path in dev so Vite proxy handles CORS (avoids cross-origin block from localhost)
VITE_PUBLIC_API_BASE_URL=/ai-api/open/
# VITE_PUBLIC_API_BASE_URL=https://www.ganjianping.com/ai-api/open/
