# Use relative path in dev so Vite proxy handles CORS (avoids cross-origin block from localhost)
VITE_OPEN_API_BASE_URL=/gjp-api/open/
# VITE_OPEN_API_BASE_URL=https://www.ganjianping.com/gjp-api/open/
